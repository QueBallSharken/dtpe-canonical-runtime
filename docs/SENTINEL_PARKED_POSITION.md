# SENTINEL PARKED POSITION (LOCKED)

## PURPOSE

This document records the current repository-authoritative parked position for Sentinel-related work.

It exists to prevent further top-level architecture drift while preserving bounded future-only material for later extraction or separate-repo handling.

---

## CURRENT POSITION

Sentinel is parked.

In the current repository, Sentinel is:

- future-only direction
- not the active runtime direction
- not the active third-wheel answer
- not the active trike-model direction
- not authorized as a normalized runtime surface by this document

Sentinel-related material currently present in this repository must be interpreted through this parked position.

---

## ACTIVE ARCHITECTURE DIRECTION

The active working architecture direction is:

- DTPE / IAL / SPECTRE = core governed execution and proof engine
- GDP = structural sufficiency sidecar
- SPECTRE-FST plus Upgrade Analysis = bounded third-wheel direction for targeted fundamental stress testing, receipt-bound classification, weak-point detection, and evidence-grounded hardening guidance

This direction is locked in:

- `docs/SPECTRE_FST_TRIKE_MODEL.md`

---

## WHY SENTINEL IS PARKED

Sentinel-related documentation expanded ahead of the now-locked trike-model architecture direction.

That material is not being treated as the active architecture priority.

The repository now treats Sentinel as:

- parked future-only material
- candidate separate-repo material
- non-authoritative for active runtime direction unless explicitly and later re-authorized

---

## WHAT THIS DOCUMENT DOES NOT CLAIM

This document does not claim:

- Sentinel has been fully removed from the repository
- Sentinel is implemented as current runtime
- Sentinel is the active answer to the current architecture expansion
- Sentinel replaces GDP
- Sentinel replaces SPECTRE-FST
- Sentinel replaces DTPE / IAL / SPECTRE

---

## CURRENT HANDLING RULE

Until further cleanup is completed:

- top-level architecture docs must follow the trike model
- Sentinel references must be interpreted as parked future-only material
- no new documentation should elevate Sentinel into the active architecture direction by implication
- no new runtime work should be justified by Sentinel naming alone

---

## CLEANUP DIRECTION

The expected cleanup direction is:

1. preserve the active trike-model architecture in top-level docs
2. park Sentinel as future-only / separate-repo candidate material
3. later extract, relocate, archive, or otherwise quarantine Sentinel package material in a controlled pass
4. avoid broad deletion without topology-aware cleanup

---

## FINAL RULE

If there is any conflict between:

- active trike-model architecture direction
- and older Sentinel-expansion wording

the active trike-model direction governs current repository interpretation.

Sentinel remains parked until explicitly reconsidered under controlled repo-first review.

END OF FILE
