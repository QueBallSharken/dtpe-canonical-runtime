DTPE Next Implementation Target

Purpose

This document defines the exact next implementation target so future
work sessions, contributors, and reviewers can continue without
reconstructing thread context.

Current confirmed state

Current implemented stable baseline:

- deterministic execution pipeline
- boundary evaluation with authority validity
- state admissibility
- system stability
- canonical receipt generation
- append-only ledger evidence
- offline verification
- replay of current boundary decision fields

Current public baseline is effectively Phase 5 complete.

Current design status

Phase 6:
- locked specification
- next public implementation target

Phase 7:
- locked design direction only
- not to be implemented in the public repo yet

Core invariant

verify without trusting the runtime that generated it

Non-negotiable rules

- No binding execution decision may depend on hidden runtime context.
- All binding decisions must be replayable from canonical recorded inputs.
- Receipts must remain sufficient for offline recomputation.
- Crypto profile handling must remain explicit and policy-governed.
- No runtime clock may be used for temporal admissibility.
- No Phase 7 implementation work may be mixed into the next public commit sequence.

Exact next implementation target

Phase 6 - Temporal Admissibility

A transition is only valid if a canonical execution_time input satisfies
its temporal constraints at the moment of evaluation.

execution_time must:

- be required at pipeline entry
- be passed unchanged through boundary -> receipt -> ledger -> verifier
- not be generated by runtime logic

The next implementation sequence must be:

Step 1
Add temporal guard and its direct unit test.

Step 2
Integrate temporal evaluation into boundary and add direct boundary-path test.

Step 3
Bind execution_time and temporal_invariant_result into pipeline and receipt,
then update pipeline/ledger assertion test.

Step 4
Update offline verifier to recompute temporal admissibility from recorded
inputs and compare deterministically.

Step 5
Update refusal-path and replay-path tests as needed.

Public repo restriction

Do not implement Phase 7 in the public repo until Phase 6 is:

- fully implemented
- fully replayable
- fully verified
- pushed in stable form

Expected next commit scope

The next engineering work should be limited to Phase 6 temporal admissibility:

- temporal guard
- boundary integration
- receipt binding
- verifier replay
- deterministic test coverage

Must not do in the next implementation cycle

- Must not use wall-clock or environment-derived decision inputs
- Must not silently infer execution_time
- Must not introduce hidden temporal state
- Must not expand into Phase 7 coherence or invariant-frame continuity
- Must not modify crypto profile semantics
- Must not publish Phase 7 implementation files

Definition of done for this target

This target is complete only when:

- execution_time is a required canonical input
- temporal_invariant_result is recorded in receipt and ledger
- offline verifier recomputes temporal admissibility using recorded inputs
- replay mismatch fails deterministically
- tests pass without relying on runtime state

END OF FILE
