# MODEL 9 / SPECTRE-SENTINEL DOCUMENT INDEX

## Status

This file is a parked index for the older bounded Model 9 / SPECTRE-SENTINEL documentation set.

It is retained for provenance, bounded navigation, and later extraction or separate-repo handling.

It is not a primary architecture entry point for the active repository direction.

---

## 1. Parked Position

This package is parked.

Use the following docs first for active repository architecture direction:

- `docs/SPECTRE_FST_TRIKE_MODEL.md`
- `docs/SENTINEL_PARKED_POSITION.md`
- `docs/GDP_RUNTIME_BRIDGE_POSITION.md`

This index exists only to preserve package topology while Sentinel-related material remains in-repo.

---

## 2. Package Scope

This index may still be used to:

- locate older Sentinel package artifacts
- preserve provenance
- support later extraction or archive work
- understand prior bounded package structure

This index must not be used to imply that the Sentinel package is an active roadmap branch.

---

## 3. Direct Rule

If there is any conflict between:

- active trike-model architecture direction
- parked Sentinel package wording

the active trike-model direction governs.

See:

- `docs/SPECTRE_FST_TRIKE_MODEL.md`
- `docs/SENTINEL_PARKED_POSITION.md`

END OF FILE
